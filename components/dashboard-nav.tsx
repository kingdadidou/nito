import Link from "next/link";
import type {Role} from "@/lib/types";
export function DashboardNav({role}:{role:Role|"user"|"organizer"|"admin"}){const normalized=role==="admin"?"administrateur":role==="organizer"?"organisateur":role==="user"?"participant":role;const common=[["/profil","Mon profil"],["/calendrier","Calendrier"],["/messages","Messagerie"]];const links=normalized==="administrateur"?[["/administration","Vue d’ensemble"],["/administration/sorties","Modération"],...common]:normalized==="organisateur"?[["/organisateur","Tableau de bord"],["/organisateur/sorties/nouvelle","Créer une sortie"],["/organisateur/onboarding","Mon dossier"],...common]:common;return <nav className="dashboard-nav" aria-label="Navigation de l’espace personnel">{links.map(([href,label])=><Link key={href} href={href}>{label}</Link>)}</nav>}

