import {describe,expect,it} from "vitest";import {canChangeCapacity,canOrganizerCancel,canReserve,reservedPlaces} from "@/lib/domain/rules";
const future="2099-08-15";const base={status:"publiee",registrationsClosed:false,date:future,organizerId:"org",userId:"participant",maximumParticipants:8,alreadyReserved:3,quantity:2};
describe("sorties",()=>{it("autorise l’annulation avant la fin",()=>{expect(canOrganizerCancel("en_attente")).toBe(true);expect(canOrganizerCancel("publiee")).toBe(true);expect(canOrganizerCancel("terminee")).toBe(false)});it("interdit de réduire la capacité sous les places confirmées",()=>{expect(canChangeCapacity(3,4)).toBe(false);expect(canChangeCapacity(4,4)).toBe(true)});});
describe("réservations et places",()=>{it("compte les réservations actives",()=>expect(reservedPlaces([{number_of_people:2,booking_status:"confirmee"},{number_of_people:1,booking_status:"annulee"},{number_of_people:3,booking_status:"en_attente"}])).toBe(5));it("accepte une réservation valide",()=>expect(canReserve(base)).toBe(true));it("refuse le dépassement de capacité",()=>expect(canReserve({...base,quantity:6})).toBe(false));it("refuse une sortie fermée",()=>expect(canReserve({...base,registrationsClosed:true})).toBe(false));it("refuse l’organisateur sur sa sortie",()=>expect(canReserve({...base,userId:"org"})).toBe(false));it("refuse une sortie non publiée",()=>expect(canReserve({...base,status:"en_attente"})).toBe(false));});
