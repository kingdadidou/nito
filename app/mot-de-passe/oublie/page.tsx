import Link from "next/link";
import { requestPasswordReset } from "@/app/connexion/actions";
export const metadata = { title: "Mot de passe oublié" };
export default async function Forgot({ searchParams }: { searchParams: Promise<{ erreur?: string; confirmation?: string }> }) { const q=await searchParams; return <section className="page-shell auth-shell"><div className="form-intro"><span className="eyebrow green">SÉCURITÉ</span><h1>Mot de passe oublié</h1><p>Nous vous enverrons un lien sécurisé pour choisir un nouveau mot de passe.</p></div><form className="big-form auth-form" action={requestPasswordReset}>{q.erreur&&<p className="form-alert error">L’e-mail n’a pas pu être envoyé.</p>}{q.confirmation&&<p className="form-alert success">Si un compte existe pour cette adresse, un e-mail vient d’être envoyé.</p>}<label>Adresse e-mail<input name="email" type="email" autoComplete="email" required/></label><button className="primary wide">Envoyer le lien</button><p className="auth-foot"><Link href="/connexion">Retour à la connexion</Link></p></form></section> }
