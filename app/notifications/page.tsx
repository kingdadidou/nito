import Link from "next/link";
import {redirect} from "next/navigation";
import {DashboardNav} from "@/components/dashboard-nav";
import {createClient} from "@/lib/supabase/server";
import {markNotificationsRead} from "./actions";
export const metadata={title:"Notifications"};
export default async function Notifications(){const supabase=await createClient();if(!supabase)return null;const user=(await supabase.auth.getUser()).data.user;if(!user)redirect("/connexion");const profile=(await supabase.from("profiles").select("user_type").eq("id",user.id).single()).data;const {data:notifications}=await supabase.from("notifications").select("id,title,content,data,read_at,created_at").eq("user_id",user.id).order("created_at",{ascending:false}).limit(50);return <section className="page-shell"><DashboardNav role={profile?.user_type??"participant"}/><div className="page-header"><div><span className="eyebrow green">ACTUALITÉS</span><h1>Notifications</h1></div><form action={markNotificationsRead}><button className="secondary">Tout marquer comme lu</button></form></div><section className="panel-card">{!notifications?.length?<p>Aucune notification.</p>:notifications.map(item=>{const tripId=typeof item.data==="object"&&item.data&&"trip_id" in item.data?String(item.data.trip_id):null;return <div className={`notification-row ${item.read_at?"":"notification-unread"}`} key={item.id}><div><b>{item.title}</b><p>{item.content}</p><small>{new Intl.DateTimeFormat("fr-FR",{dateStyle:"medium",timeStyle:"short"}).format(new Date(item.created_at))}</small></div>{tripId&&<Link className="secondary" href={`/sorties/${tripId}`}>Voir la sortie</Link>}</div>})}</section></section>}
