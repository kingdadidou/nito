"use server";
import {revalidatePath} from "next/cache";
import {redirect} from "next/navigation";
import {requireRole} from "@/lib/auth";
import {createAdminClient} from "@/lib/supabase/admin";
import {sendTransactionalEmail} from "@/lib/email/server";
export async function moderateReview(form:FormData){const adminUser=await requireRole(["administrateur"]);if(!adminUser)redirect("/connexion");const id=String(form.get("review_id")??"");const status=String(form.get("status")??"");if(!/^[0-9a-f-]{36}$/i.test(id)||!["publie","masque"].includes(status))redirect("/administration/avis?erreur=action");const admin=createAdminClient();const {data:review,error}=await admin.from("reviews").update({moderation_status:status,moderated_at:new Date().toISOString(),moderated_by:adminUser.id}).eq("id",id).select("recipient_id,author_id,author:profiles!reviews_author_id_fkey(email,first_name)").single();if(error)redirect("/administration/avis?erreur=action");await admin.from("admin_audit_logs").insert({admin_id:adminUser.id,action:status==="masque"?"hide_review":"publish_review",entity_type:"review",entity_id:id,details:{recipient_id:review.recipient_id}});const author=Array.isArray(review.author)?review.author[0]:review.author;if(author?.email)await sendTransactionalEmail({eventKey:`review-moderation:${id}:${status}`,to:author.email,userId:review.author_id,template:"moderation_decision",subject:"Décision concernant votre avis",heading:status==="masque"?"Votre avis a été masqué":"Votre avis a été republié",content:`Bonjour ${author.first_name||""},\nVotre avis a été ${status==="masque"?"masqué après contrôle de la modération":"republié"}.`,actionLabel:"Voir mes avis",actionUrl:`${process.env.NEXT_PUBLIC_SITE_URL}/avis`});revalidatePath("/administration/avis");revalidatePath(`/organisateurs/${review.recipient_id}`);redirect("/administration/avis?succes=1")}
