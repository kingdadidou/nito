import Link from "next/link";
import {DashboardNav} from "@/components/dashboard-nav";
import {createAdminClient} from "@/lib/supabase/admin";
export default async function Admin(){const admin=createAdminClient();const [{count:users},{count:active},{count:bookings},{count:reports},{count:documents},{count:disputes},{count:reviews}]=await Promise.all([admin.from("profiles").select("id",{count:"exact",head:true}),admin.from("trips").select("id",{count:"exact",head:true}).eq("status","publiee"),admin.from("bookings").select("id",{count:"exact",head:true}),admin.from("reports").select("id",{count:"exact",head:true}).in("status",["ouvert","en_cours"]),admin.from("organizer_documents").select("id",{count:"exact",head:true}).eq("status","en_attente"),admin.from("payment_disputes").select("id",{count:"exact",head:true}).not("status","in",'(won,lost,warning_closed)'),admin.from("reviews").select("id",{count:"exact",head:true})]);const cards=[["Sorties à modérer","Contrôlez les propositions avant publication.","/administration/sorties"],["Justificatifs organisateurs",`${documents??0} document(s) à contrôler.`,"/administration/documents"],["Réservations et remboursements","Consultez les réservations et déclenchez les remboursements autorisés.","/administration/reservations"],["Finances Stripe",`${disputes??0} litige(s) Stripe ouvert(s).`,"/administration/finances"],["Avis",`${reviews??0} avis publié(s) à superviser.`,"/administration/avis"],["Signalements",`${reports??0} signalement(s) à traiter.`,"/administration/signalements"],["Utilisateurs","Suspendez ou réactivez un compte avec traçabilité.","/administration/utilisateurs"],["Journal administratif","Consultez l’historique des décisions sensibles.","/administration/journal"]];return <section className="page-shell"><DashboardNav role="administrateur"/><span className="eyebrow green">ADMINISTRATION NITO</span><h1>Tableau de bord</h1><div className="admin-kpis"><div><span>Utilisateurs</span><strong>{users??0}</strong></div><div><span>Sorties actives</span><strong>{active??0}</strong></div><div><span>Réservations</span><strong>{bookings??0}</strong></div><div><span>Documents à contrôler</span><strong>{documents??0}</strong></div></div><div className="admin-grid">{cards.map(([title,description,href])=><section className="panel-card" key={href}><h2>{title}</h2><p>{description}</p><Link className="primary" href={href}>Ouvrir</Link></section>)}</div></section>}
